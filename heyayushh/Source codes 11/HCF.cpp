// This program find the greatest common divisor of two numbers.
#include <iostream>
using namespace std;

int main()
{
    int r, x, y;
    cout << "Enter higher no -> ";
    cin >> x;
    cout << "enter no.";
    cin >> y;

    r = x % y;
    if (r==0)
    {
        cout<<"heighest common factor of"<<x<<" and "<<y<<"is"<<y;
    }
    else
    { 
		cout<<"heighest common factor of"<<x<<" and "<<y<<"is"<<r;
    }
    if (r=float)
{
	cout<<"test";
}
return 0;
}
